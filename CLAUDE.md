# Development Guidelines

## Branching

- Always create a new Git branch for each task or feature before making changes.
- Never commit directly to `master`.
- Use descriptive branch names that reflect the task (e.g., `claude/feature-name-sessionId`).
